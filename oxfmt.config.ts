import { defineConfig } from 'oxfmt';

export default defineConfig({
  useTabs: false,
  tabWidth: 2,
  singleQuote: true,
  trailingComma: 'es5',
  printWidth: 100,
  proseWrap: 'always',
  jsdoc: true,
});
